"""Test Automation Studio services.

Split by screen: `intake_service` and `coverage_service` back Screen 1,
`refinement_service` and `classification` back Screen 2, `script_lab_service`
backs Screen 3, and `test_data_bridge` connects all of them to the shared
Test Data module.
"""
